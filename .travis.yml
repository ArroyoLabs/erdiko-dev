sudo: required

install:
  - curl -sSL https://get.docker.com/ | sudo sh
  - sudo pip install docker-compose

script:
  - ls -lah
  - docker-compose up -d
  - docker-compose ps
  - docker exec erdiko_php7 ls -lah

notifications:
  email:
    - john@arroyolabs.com 
